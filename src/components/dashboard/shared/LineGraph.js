import React, { Component } from 'react';

export default class LineGraph extends Component {
  render() {
    return (
      <div className="graph">
        insert line graph stuff here
      </div>
    );
  }
}