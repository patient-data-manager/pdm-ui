// import { expect } from '../../utils/test_helper';
// import healthRecord from '../../reducers/health_record_reducer';
// import healthRecordData from '../data/fhir_bundle.json'

// import {
//   FETCH_PATIENT_EVERYTHING_FULFILLED
// } from '../../actions/types';

describe('health record reducer', () => {
  xit('should fetch and group health record', () => {
    // const payloadData = {data: healthRecordData };
    // const action = {type: FETCH_PATIENT_EVERYTHING_FULFILLED, payload: payloadData};
    // const startState = {}
    // const nextState = healthRecord(startState, action);
    // expect(nextState.Condition.length).to.equal(2);
    // expect(nextState.Observation.length).to.equal(32);
    // expect(nextState.Encounter.length).to.equal(7);
  });
});
